getUserDetails:
  handler: lambda/getUserDetails.handler
  events:
    - http:
        path: /getUserDetails
        method: post
        cors: true
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId: !Ref ApiAuthorizer
